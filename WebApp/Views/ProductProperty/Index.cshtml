@model IEnumerable<CafeMenuProject.Entities.Admin.ProductProperty>
<h2>PRODUCTPROPERTY</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PropertyId)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PropertyId)
            </td>
            <td>
                @Html.ActionLink("Update", "Update", new {  id=item.ProductPropertyId }) |

                @Html.ActionLink("Delete", "Delete", new {  id=item.ProductPropertyId }) |

            </td>
        </tr>
    }

</table>